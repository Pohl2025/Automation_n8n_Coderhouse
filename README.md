El CRM se simuló con Airtable (conector nativo con autenticación por token y scopes acotados a contactos read/write)
por restricciones de acceso a HubSpot. 
La lógica Look up → Create/Update es idéntica a la que se aplicaría sobre un CRM comercial
